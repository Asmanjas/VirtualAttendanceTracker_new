<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aptech.asmanjas.virtualattendancetracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_student" />
        <activity android:name=".register_student" />
        <activity android:name=".login_faculty" />
        <activity android:name=".register_faculty" />
        <activity android:name=".gLoginActivity" />
        <activity android:name=".SignUpG" />
        <activity android:name=".gLoginScreenActivity" />
        <activity android:name=".enter_past_attendance"/>
        <activity android:name=".MainActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDL6xBkphKNsNuKjC36s8HpkepowgITgDU" />

        <activity android:name=".EnterTimeTableG" />
        <activity android:name=".NumberOfSubjects" />
        <activity android:name=".subjectNameG" />
        <activity android:name=".Monday" />
        <activity android:name=".mainModuleG" />
        <activity android:name=".Current_location_tracker" />
        <activity android:name=".Module_student" />
        <activity android:name=".Module_faculty" />
        <activity android:name=".showAttendance" />
        <activity android:name=".enterDetailsG" />
        <activity android:name=".Tuesday" />
        <activity android:name=".Wednesday" />
        <activity android:name=".Thursday" />
        <activity android:name=".Friday" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".showAttendanceG" />

        <service android:name=".AttendanceCalculationService" />

        <activity android:name=".enter_past_attendance"></activity>
    </application>

</manifest>